# Watermark come with each driver, and can be referenced from PPDs,
# so we put them in a driver-specific location.
@DRIVER_NAME@/watermark /usr/share/epson-inkjet-printer/@DRIVER_NAME@/

# Resource files will be looked up by the CUPS filter and need
# to be placed in a common place, regardless of the driver.
@DRIVER_NAME@/resource  /usr/share/epson-inkjet-printer/

# Put PPDs in a common location too for CUPS to easily find them
@DRIVER_NAME@/ppds      /usr/share/epson-inkjet-printer/
